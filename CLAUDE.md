# Монорепо: Mobile App + Backend

## Структура
- `client/` — Expo + NativeWind (React Native)
- `server/` — NestJS + Prisma

## Важливо
- Дизайн-задачі стосуються ТІЛЬКИ client/
- Ніколи не чіпай server/ коли працюєш з UI
- API контракти (типи, ендпоінти) не змінювати